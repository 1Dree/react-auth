import signup from "./requisitors/signup";
import login from "./requisitors/login";
import signout from "./requisitors/signout";
import updateProfile from "./requisitors/updateProfile";
import newPassword from "./requisitors/newPassword";

const API = {
  signup,
  login,
  signout,
  updateProfile,
  newPassword,
};

export default API;
